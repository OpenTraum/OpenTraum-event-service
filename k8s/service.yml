apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: opentraum
  labels:
    app: event-service
    app.kubernetes.io/part-of: opentraum
spec:
  selector:
    app: event-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
